{
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist",
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "lint": "echo \"lint placeholder\""
  },
  "type": "module",
  "types": "./dist/index.d.ts",
  "version": "0.1.0",
  "devDependencies": {
    "typescript": "^5.8.0",
    "rimraf": "^6.0.0",
    "vitest": "^3.2.0",
    "fastify": "^5.2.0"
  },
  "peerDependencies": {
    "fastify": "^5.0.0"
  },
  "name": "@instantmockapi/auth",
  "private": true,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "dependencies": {
    "@instantmockapi/shared": "workspace:*",
    "@instantmockapi/config": "workspace:*",
    "fastify-plugin": "^5.0.1",
    "jose": "^6.0.10"
  }
}
